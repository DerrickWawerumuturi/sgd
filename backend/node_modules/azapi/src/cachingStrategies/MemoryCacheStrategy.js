let cache = {};

export default Object.assign(
  function (options) {
    return {
      get,
      set,
    };
  },
  {
    reset() {
      cache = {};
    },
  },
);

function get(name) {
  return cache[name];
}

function set(name, value) {
  cache[name] = value;
}
