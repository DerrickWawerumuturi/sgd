import useContext from './useContext';

export default function useParams(name) {
  if (arguments.length) {
    return useParams()[name];
  }

  const context = useContext();
  if (!context.__params) {
    context.__params = {
      ...context.req.query,
      ...context.req.body,
    };
  }
  return context.__params;
}
