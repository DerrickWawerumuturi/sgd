import useRequest from './useRequest';

const defaultBody = {};

export default function useBody(name) {
  if (arguments.length) {
    return (useBody() || defaultBody)[name];
  }
  return useRequest().body;
}
